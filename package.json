{
  "name": "downloadhelper",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Local-first Chromium media downloader extension",
  "scripts": {
    "test": "node --test",
    "build": "node scripts/build.mjs",
    "check": "node scripts/check.mjs"
  },
  "engines": {
    "node": ">=22"
  }
}
